{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "command": "g++ -o main -g $(find ${workspaceFolder} -type f | grep \"\\.cpp\")",
            "type": "shell",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}